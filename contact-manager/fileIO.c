#include <stdio.h>
#include <stdlib.h>
#include "customLib.h"


void loadFiles(node *temp) {

    FILE *file = fopen("credentials.txt", "r");

    // while (fscanf(file, "%49[^|]|%19[^|]|%49[^|]|%59[^\n]",  )) {


    // }

    return;
}